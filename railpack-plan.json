{
    "language": "Python",
    "python_version": "3.12",
    "build": "python3 -m ensurepip && python3 -m pip install --upgrade pip && python3 -m pip install -r backend/requirements.txt",
    "start": "python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8080",
    "watch": [ "backend/", "agent/" ],
    "env": {
        "PYTHONUNBUFFERED": "1"
    }
}
